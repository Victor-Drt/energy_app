import 'package:flutter/material.dart';

class PageComparativo extends StatelessWidget {
  const PageComparativo({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Comparativos"),
      ),
    );
  }
}